import {DatasetMapping, Suggestion} from "../models.ts";

export const getSuggestions = (additionalDatasetMappings: DatasetMapping[]): Suggestion[] => {
    console.log(additionalDatasetMappings)
    return []
}